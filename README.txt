Each of these folders contain a capture the flag challange. Flags are strings that are unique to each challange 
{Th15_is_A_fLaG}
Attempt to find all the flags.
Some challanges have challanges hidden inside them, so keep your eyes peeled.
